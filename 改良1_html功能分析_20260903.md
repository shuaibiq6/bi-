# 改良1.html 功能分析文档

> 分析对象：[改良1.html](改良1.html)（供应链综合看板 · 暖色规范 · 数据增强版）
> 生成日期：2026-09-03
> 说明：本文件为纯静态单页应用，无后端、无数据库，所有数据均来自本地导入的 Excel 文件。

---

## 一、总体架构

| 维度 | 说明 |
|---|---|
| 形态 | 单个 HTML 文件，双击即可在浏览器打开 |
| 前端库 | SheetJS `xlsx@0.18.5`（Excel 解析）+ Chart.js `4.4.0`（图表），均走 CDN |
| 运行时 | 浏览器端原生 JavaScript，无构建、无框架、无依赖安装 |
| 数据来源 | 用户手动导入 3 类 Excel：`采购单`、`采购入库管理`、`库存查询` |
| 数据存储 | 内存变量 `appData`（页面刷新即丢失，不持久化） |

核心数据流：

```
点击「导入数据」→ 选择 Excel → FileReader 读取字节
  → XLSX.read 解析 → fixSheetRange 修复列范围
  → sheet_to_json 转对象数组 → detectFileType 识别类型
  → 写入 appData{ purchaseOrders / inboundRecords / inventory }
  → refreshData() → 各 renderXxxSection() 计算指标 + 渲染表格 + 绘制图表
```

---

## 二、页面 UI 结构

页面分为：**顶栏、状态栏、导航标签、主内容区（5 个 section）、提示气泡、加载遮罩**。

| 区域 | 元素/ID | 作用 |
|---|---|---|
| 顶栏 | `.header-actions` | 4 个操作按钮：刷新 / 导入 / 导出Excel / 导出PDF，及当前日期 |
| 状态栏 | `#statusText` / `#recordCount` | 显示导入状态与已加载记录总数 |
| 导航标签 | `.nav-tab` | 库存健康度 / 待下单清单 / 在途跟单 / 仓库货值（锚点跳转） |
| 加载遮罩 | `#loadingOverlay` | 打开页面 800ms 后自动隐藏 |
| 提示气泡 | `#toast` | 导入成功/失败反馈 |

隐藏文件输入框 `#fileInput`（`multiple` + `accept=".xlsx,.xls"`）挂在顶栏，由「导入数据」按钮触发 `triggerImport()` 点击。

---

## 三、功能模块逐一分析

### 1. 文件导入（`triggerImport` / `handleFileImport` / `fixSheetRange`）

- **`triggerImport()`**：模拟点击隐藏的 `#fileInput`，弹出文件选择框。
- **`handleFileImport(event)`**：遍历用户选择的文件，每个文件用 `FileReader.readAsArrayBuffer` 读取，`XLSX.read` 解析第一个工作表，`sheet_to_json({defval:''})` 转成对象数组；全部处理完后统一调用 `refreshData()`，并弹 toast 反馈。
- **`fixSheetRange(ws)`**（本次优化新增，关键修复）：原始导出文件内部 `<dimension>` 只声明了 1 列（`A1:A5375`），导致 SheetJS 只读出「序号」一列。此函数根据真实存在的单元格坐标重算 `ws['!ref']`，修复列丢失。

### 2. 文件类型自动识别（`detectFileType`）

按优先级识别：**文件名** → **列名特征**。

| 类型 | 文件名规则 | 列名特征 |
|---|---|---|
| `purchaseOrder`（采购单） | 含「采购单」且不含「入库」 | 含「采购单状态」+「采购总金额（元）」 |
| `inbound`（采购入库） | 含「入库」 | 含「入库单号」+「入库单状态」 |
| `inventory`（库存查询） | 含「库存」 | 含「可用库存」+「成本价」 |

### 3. 列名映射（`COLUMN_MAPS`）

三套映射表，精确对应各 Excel 表头（中文），实现「表头→代码 key」解耦：

- `purchaseOrder`：39 列（货品编码/采购单号/采购数量/待入库金额/采购金额（明细）/入库正品数量…）
- `inbound`：33 列（入库单号/入库数量/入库价格/入库金额/供应商…）
- `inventory`：15 列（可用库存/待采购量/采购在途/正品库存/次品库存/总成本/成本价…）

### 4. 数据刷新（`refreshData`）

汇总 3 类数据总记录数写入 `#recordCount`；调用 5 个渲染函数：`renderInventorySection`、`renderTodoSection`、`renderTransitSection`、`renderWarehouseSection`、`renderInboundSection`。

### 5. 库存健康度（`renderInventorySection`）

- **指标卡**：现有库存货值 `invValue`、总库存货值 `invTotalValue`、有效SKU数 `invSkuCount`、零库存 `invStagnantCount`、负库存 `invNegativeCount`、健康 `invHealthyCount`。
- **口径**：货值 = `正品库存 × 成本价`；负库存 = `可用库存 < 0`；零库存 = `可用库存 ≤ 0`；健康 = `可用库存 > 0`。
- **图表**：库存状态分布（环形）、仓库库存分布（条形）、库存货值 TOP10（条形）。
- **明细表**：按货值降序前 30 行。

### 6. 待下单清单（`renderTodoSection`）

- **优先级规则**：P0 = `可用库存 < 0`；P1 = `可用库存 = 0` 且 `待采购量 > 0`；P2 = `待采购量 > 0` 且 `可用库存 > 0`。
- **指标卡**：P0/P1/P2/合计。
- **图表**：采购优先级分布（环形）、各优先级待采购量合计（条形）。
- **明细表**：合并 P0+P1+P2 前 30 行，带优先级徽章。

### 7. 在途跟单（`renderTransitSection`）

- 筛选状态为「已审核」「部分入库」的采购单，按采购单号去重统计单数。
- **延迟判定**：`预计到货时间 < 今天` 且 `入库正品数量 < 采购数量`，计算延迟天数。
- **指标卡**：在途采购单数、在途采购金额、延迟明细数、延迟单数。
- **图表**：采购单状态分布（环形）、在途金额按供应商 TOP10（条形）。
- **明细表**：延迟/未完全入库明细前 30 行（按延迟天数降序）。

### 8. 仓库货值（`renderWarehouseSection`）

- **指标卡**：现有库存货值 `whCurrentValue`、在途货值 `whTransitValue`（来自采购单）、有库存SKU货值 `whActiveValue`、零库存SKU数 `whZeroValue`。
- **图表**：SKU 货值 TOP20（条形）、仓库货值占比（饼图）。
- **明细表**：按货值降序前 30 行。

### 9. 采购入库记录（`renderInboundSection`）

- **指标卡**：入库单总数 `inboundCount`、入库总金额 `inboundAmount`、入库总数量 `inboundQty`。
- **多规格处理（本次优化新增）**：入库明细中「入库数量/入库价格/入库金额」存在 `"90: 135"` 形式的多规格单元格。新增 `sumColon()` 工具函数，逐项求和，修正此前 `parseFloat` 只取第一个数导致的金额/数量低估。
- **明细表**：前 30 行，如实展示多规格原始值。

### 10. 图表渲染（`renderChart`）

统一封装：销毁旧实例 → 取 canvas → `new Chart()`。非饼/环形图统一坐标轴样式，饼/环形图 legend 置底。

### 11. 导出功能

- **`exportExcel()`**：将内存中 3 类数据用 `json_to_sheet` 生成 3 个工作表，导出 `供应链看板数据导出.xlsx`。
- **`exportPDF()`**：仅提示并调用 `window.print()`，依赖浏览器打印。

### 12. 工具函数

| 函数 | 作用 |
|---|---|
| `formatWan(v)` | ≥1 万显示「xx.xx万」，否则「xx.xx元」 |
| `sumColon(v)` | 汇总 `"90: 135"` 多规格单元格的全部数值（本次新增） |
| `parseDate(str)` | 字符串/Date 转 Date 对象 |
| `formatDate(str)` | 格式化日期为 `zh-CN` 短格式 |
| `showToast(msg, type)` | 顶部右侧提示气泡，3 秒自动消失 |

---

## 四、本次优化修复记录

| 问题 | 根因 | 修复 |
|---|---|---|
| 导入后大面积不显示数据 | 原始 Excel `<dimension>` 只声明 1 列，SheetJS 只读出「序号」一列 | 新增 `fixSheetRange()`，解析前重算 `!ref` |
| 入库总金额/数量被低估 | 多规格单元格 `"90: 135"` 用 `parseFloat` 只取首数 | 新增 `sumColon()`，入库金额/数量/价格逐项求和 |

**验证**：入库总金额 1373.43万 → 1761.44万；入库总数量 623,615 → 944,830，与 openpyxl 独立计算完全一致。

---

## 五、已知问题与改进建议（不影响核心展示）

1. **「总库存货值(含在途)」指标与「现有库存货值」相同**：`invTotalValue` 目前只取 `Σ(正品库存×成本价)`，未真正叠加「采购在途」货值，标签与计算口径不符。建议补充在途货值，或改标签。
2. **`.btn-trend` 样式为死代码**：顶栏并无「趋势」按钮，CSS 未使用。
3. **「采购入库记录」无导航标签**：导航只有 4 个 tab，第 5 个 section 恒显示在底部，无锚点跳转。
4. **导航为纯锚点滚动**：无 tab 切换显隐逻辑，`active` 仅视觉高亮。
5. **各明细表仅展示前 30 行**：大数据量时其余行不可见，无分页/搜索。
6. **在途「延迟明细」混入未完全入库但未超期的记录**：判定条件 `isDelayed || inboundQty < qty` 将「未超期但未完全入库」也计入延迟明细。
7. **数据仅存内存**：刷新页面即丢失，需重新导入（这正是后文「两日对比」需引入 IndexedDB 的原因）。

---

## 六、测试结果汇总（Edge 无头浏览器 + 真实文件导入）

对 **原始数据 + 3 组测试数据** 共 4 组分别导入，全部通过：9 个图表渲染、5 张表填充、所有指标非空、无任何 JS 异常。

| 指标 | 原始数据 | 测试组1 | 测试组2 | 测试组3 |
|---|---|---|---|---|
| 记录数 | 13,107 | 4,368 | 4,368 | 4,371 |
| 库存货值 | 478.49万 | 255.74万 | 166.27万 | 56.48万 |
| 待下单 P0/P1/P2 | 114/7/48 | 51/4/34 | 50/3/11 | 13/0/3 |
| 在途金额 | 289.33万 | 216.04万 | 60.25万 | 13.05万 |
| 入库总金额 | 1761.44万 | 456.24万 | 630.81万 | 674.38万 |
| 入库总数量 | 944,830 | 308,623 | 324,872 | 311,335 |

> 交叉校验：三组入库金额之和 ≈ 1761.43万（≈ 原始 1761.44万，差值为四舍五入）；三组入库数量之和 = 944,830，与原始完全一致，证明多规格求和正确。

**最终保留**：`表格数据/测试组1_采购单.xlsx`、`测试组1_入库.xlsx`、`测试组1_库存.xlsx`（其余测试组已删除）。
